import { Copyright } from '@ui';

export default function Footer() {
  return (
    <footer className="wrapper flex items-center justify-center py-6 md:py-10">
      <Copyright />
    </footer>
  );
}
